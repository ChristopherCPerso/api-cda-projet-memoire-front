import type { Config } from "tailwindcss";

// Tailwind v4 n'utilise plus `content` ni `theme.extend` ici pour les polices.
// Toute la personnalisation se fait via `@theme` dans le CSS.
const config: Config = {
  // Laissez vide ou ajoutez des plugins si besoin
  plugins: [],
};

export default config;
